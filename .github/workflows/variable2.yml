name: Vars Variable 1

on:
 workflow_dispatch
env:
 MY_VAR: workflow
jobs:
 variables:
  env:
   MY_VAR: jobs
  runs-on: ubuntu-latest
  steps:
  - name: Print
    run: |
     echo 'format('Hello {0} {1} {2}', 'Mona', 'the', 'Octocat')'
    env:
     MY_VAR: steps
